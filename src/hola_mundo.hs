import GHC.Base
import PdePreludat

main :: IO () -- Con esta linea indicamos que el "main" va realizar una operación del tipo "IO" (Input y Output).
main = putStrLn "Hello World" -- El "main" retornara con un string al ponerlo en la consola.